import { SetupScreen } from "@/components/subscriptions/setup-screen";

export const metadata = {
  title: "Tell Tide | Setup",
  description: "Connect a Reown wallet and program Tell Tide subscriptions."
};

export default function SetupPage() {
  return <SetupScreen />;
}
